{"data":{"diff":"diff --git a/crates/plugkit-core/src/orchestrator/instructions/prose/entry.md b/crates/plugkit-core/src/orchestrator/instructions/prose/entry.md\nindex 5a07254..40d6db7 100644\n--- a/crates/plugkit-core/src/orchestrator/instructions/prose/entry.md\n+++ b/crates/plugkit-core/src/orchestrator/instructions/prose/entry.md\n@@ -43,7 +43,7 @@ Thread SESSION_ID through every spool body; plugkit rejects empty.\n \n ## Inspection routing\n \n-Every capability has exactly one sanctioned surface and the platform's native tools are never it: code/file/symbol search AND filename/path lookup (`codesearch` body `{\"query\":\"<glob or substring>\",\"mode\":\"filename\"}`, matched against every tracked path in cwd regardless of language-index coverage) are both the `codesearch` verb, defaulting to cwd but never confined to it -- `codesearch {root|projectPath: \"<abs>\", query, mode?}` targets any folder (a submodule, a sibling repo, any other project on disk), its own persistent index/cache living at `<root>/.gm/gm.db`, isolated from and reusable independent of the current project's own index; a sibling repo or submodule is never `Read`-by-path scanned or shelled out to `find`/Grep/Glob just because it sits outside cwd -- pass `root`/`projectPath` instead. Runtime-state files (spool response JSON, `.status.json`) are `Read`, browser automation of any kind is the `browser` verb (no raw Chrome launch, no puppeteer/playwright import or CLI, ever -- same inadmissible-reach class as bypassing `codesearch`), and Bash survives only for the boot probe and shell-only non-git tooling (`curl`, `sh`, `pwsh`). Reaching for Glob/Grep/find/Explore or any host-native search is reaching around the surface -- it is blocked; the verb IS the surface, regardless of whether the target is cwd or an external root. Spool responses are synchronous; poll external state via `until <check>; do sleep N; done`.\n+Every capability has exactly one sanctioned surface and the platform's native tools are never it: code/file/symbol search AND filename/path lookup (`codesearch` body `{\"query\":\"<glob or substring>\",\"mode\":\"filename\"}`, matched against every tracked path in cwd regardless of language-index coverage) are both the `codesearch` verb, defaulting to cwd but never confined to it -- `codesearch {root|projectPath: \"<abs>\", query, mode?}` targets any folder (a submodule, a sibling repo, any other project on disk), its own persistent index/cache living at `<root>/.gm/gm.db`, isolated from and reusable independent of the current project's own index; a sibling repo or submodule is never `Read`-by-path scanned or shelled out to `find`/Grep/Glob just because it sits outside cwd -- pass `root`/`projectPath` instead. Runtime-state files (spool response JSON, `.status.json`) are `Read`, browser automation of any kind is the `browser` verb (no raw Chrome launch, no puppeteer/playwright import or CLI, ever -- same inadmissible-reach class as bypassing `codesearch`), and the registered Bash surface survives only for the boot probe and shell-only non-git tooling. Reaching for Glob/Grep/find/Explore or any host-native search is reaching around the surface -- it is blocked; the verb IS the surface, regardless of whether the target is cwd or an external root. Spool responses are synchronous; poll external state via `until <check>; do sleep N; done`.\n \n **`codesearch` has four modes, and it refuses any other value.** `dual` is the default: ranked BM25 plus vector retrieval, for \"where is the code that does X\". `literal` and `regex` are EXHAUSTIVE, for \"every place this exact text appears\" -- a definition-and-call-site sweep, a rename audit, a call-graph trace, a \"what calls Y\" question. They return every match with `path` and `line`, in tree order, with no relevance ranking and no top-k cut. They read the tree directly and skip the index, the embedder and the corpus digest, so they answer in about one second where `dual` on the same query over a large workspace costs minutes (measured on a 1777-file Rust workspace: `literal` returned all 16 `set_times_at` matches in 1.0s; `dual` on the identical query took 317s and produced no usable answer). `filename` matches paths only. An unknown mode is an error that names the valid set -- it is never served as `dual`, which is what used to happen, and a ranked 10-hit answer then read as an exhaustive one.\n \n","range":"","truncated":false},"dispatch_id":"1789088334109-6-70da00c6d187162","next_dispatch_hint":"instruction","ok":true,"request_fingerprint":"b2570c54fc3f0464","verb":"git_diff"}