---
key: mem-e21897ef09fd9f92-1913
ns: default
created: 1783347305952
updated: 1783347305952
---

## Resolved mutable: host-sql-mechanism-shape

src/wasm_dispatch.rs:745 pub struct PlugkitKv; src/wasm_dispatch.rs:747-764 impl rs_learn::KvBackend for PlugkitKv wrapping host_kv_get/put/query (externs declared in THIS file, not rs-learn); src/wasm_dispatch.rs:766-774 fn learn() constructs rs_learn::LearnSession::new(PlugkitKv) -- a plain Rust value passed by rs-plugkit into rs-learn's public constructor. rs-learn's Cargo.toml has zero dependency on rs-plugkit; it only defines the KvBackend trait shape rs-plugkit's struct must satisfy. This is already a working non-circular plain-Rust cross-crate call: rs-plugkit (the binary/cdylib owner) depends on rs-learn, implements rs-learn's trait locally, and injects the impl at construction time. No wasm host-import round trip is involved in this existing pattern at all -- host_kv_* are merely what PlugkitKv's methods happen to delegate to today, an implementation detail of the struct, not a requirement of the pattern. Therefore the correct non-circular mechanism for rs-learn code to use the SAME libsql instance is: rs-learn's own (out-of-repo, future) session defines a new trait (e.g. SqlBackend) mirroring KvBackend's shape, and rs-plugkit implements it on a new or the same PlugkitKv-like struct backed by libsql_wasm::exec/query/exec_params/query_params against GM_DB -- then passes it into LearnSession::new (or an rs-learn API accepting it) exactly as KvBackend is injected today. This requires zero new wasm host-import externs and zero circular dependency; it only requires rs-plugkit to expose Rust helper fns (shared_db.rs) that a trait-impl struct can call, ready for rs-learn's session to define the trait and accept the impl. Original task framing (new host_sql_* JS-side imports) is supplanted by this stronger, already-precedented, lower-friction mechanism discovered via codesearch of the ACTUAL existing KvBackend wiring, not assumption.
