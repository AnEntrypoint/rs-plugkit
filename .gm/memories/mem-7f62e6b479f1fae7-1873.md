---
key: mem-7f62e6b479f1fae7-1873
ns: default
created: 1783349369240
updated: 1783349369240
---

## Resolved mutable: shared-db-inmemory-not-persisted

Confirmed decisive: SHARED_DB ("gm") opens at literal ":memory:" (code_index.rs:187-190 project_db_path ignores its project_path arg entirely). This means code_chunks/memories/pipeline_state -- and any new rssearch_vectors table built on shared_exec/shared_query -- are wiped on every wasm instance re-instantiation (watcher restart/version bump/process respawn, observed twice already this session via the update-triggered reboot). The existing code_index.rs vector-table pattern (codesearch_libsql verb, code_index.rs::search()) already exists as an alternative to host_vec_search but is NOT the live path the standard codesearch verb dispatches to (wasm_dispatch.rs:1696 routes to codesearch() at line 530, which calls host_vec_search, the disk-persisted JS flat-file store) precisely because codesearch_libsql's backing store is the same in-memory GM_DB and would not survive a restart. This is the root reason the requested migration (host_vec_search -> rssearch_vectors via shared_exec/shared_query) cannot be a safe drop-in replacement today: it would trade a disk-durable store (JSON files under .gm/disciplines/<ns>-vec/, survives every watcher restart) for a memory-only one (wiped on every restart), regressing recall/codesearch/memorize durability for this exact repo's own live gm session. Prerequisite (out of migration-of-search-backend scope): wire project_db_path to a real .gm/-relative sqlite file path BEFORE any read/write path is switched, and verify open() with SQLITE_OPEN_CREATE against a real file survives a process restart via exec_js witness. Until that prerequisite lands and is witnessed, host_vec_search must remain the live read/write path for recall/codesearch/memorize; rssearch_vectors can be built and dual-written now (harmless, additive) but must NOT become the sole read source.
