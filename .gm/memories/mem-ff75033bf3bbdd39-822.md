---
key: mem-ff75033bf3bbdd39-822
ns: default
created: 1783389523033
updated: 1783389523033
---

## Resolved mutable: vec-cutover-inmemory-hardcode-race

Duplicate/orphan entry of mut-1783387310594 (same id-ignored-under-concurrent-writer quirk already on file as prd-add-id-ignored-recurrence-concurrent-writer, now confirmed also affecting mutable-add: the literal id string passed in the add body was stored as its own separate unknown-status row alongside the system's auto-generated mut-1783387310594 id, which already carries the real fix witness). Same underlying fix: src/rssearch_vectors.rs ensure_schema() now calls shared_ensure_open(&shared_db_path()) (real gm.db path via code_index::project_db_path(None)) instead of shared_ensure_open(':memory:'). Verified via cargo check --target wasm32-wasip1 (0 errors) and live sql_query witness showing rssearch_vectors data (561+17 rows) surviving watcher restart.
