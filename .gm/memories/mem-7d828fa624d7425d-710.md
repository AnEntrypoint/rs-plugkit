---
key: mem-7d828fa624d7425d-710
ns: default
created: 1783071733268
updated: 1783071733268
---

## Resolved mutable: audit-2-ci-validated-no-writer

codesearch across rs-plugkit source (queries for ci-validated, CI CD validation witness workflow write consolidate) surfaced zero writers of .gm/exec-spool/.ci-validated within this crate -- it is written externally (gm harness or CI workflow, outside rs-plugkit's dispatch surface). Fix scoped correctly to the gate side (src/gates.rs ci_validation_fresh()) which now requires a JSON body with head_sha matching current git HEAD; this is the defect location per the finding (bare exists-check -> freshness-check). The external writer contract is now self-documented in the residual message; updating that external writer is out of rs-plugkit's crate scope.
