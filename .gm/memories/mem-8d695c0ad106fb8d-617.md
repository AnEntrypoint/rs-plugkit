---
key: mem-8d695c0ad106fb8d-617
ns: default
created: 1783387927348
updated: 1783387927348
---

## Resolved mutable: mut-1783387310594

src/rssearch_vectors.rs: added shared_db_path() calling crate::code_index::project_db_path(None) (returns 'gm.db', pub(crate)-exposed in code_index.rs:195), ensure_schema() now calls shared_ensure_open(&shared_db_path()) instead of shared_ensure_open(':memory:'). Confirmed both rssearch_vectors and code_index now resolve the SAME real on-disk path regardless of dispatch order, eliminating the idempotent-open race. cargo check --target wasm32-wasip1 --no-default-features --features wasm --lib succeeded with this change (2m11s, 0 errors, 4 pre-existing unrelated warnings).
