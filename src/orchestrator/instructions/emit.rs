pub const TEXT: &str = r#"# EMIT — L3 audit on disk

Intent → artifact. The whole covering family lands, not a representative subset. Emit the closure on first write; partial emits are non-monotonic.

## Read-before-write — distance check on disk

Read target path before write. Diff against an unread file is diff against a stale model — measuring the candidate against an imagined baseline. Mismatch → snake to PLAN. The on-disk file IS the goal-relative reference; the write is the mutation; the post-write Read is the audit closure.

## Fresh index

Codeinsight + search outputs feed EMIT only from a freshly-completed index. Digest match against live fs or no result. Emitting from an unverified index is bluffing the cost layer — agent acts on a state that no longer exists.

## Write-then-verify — central store + checksum

One Edit/Write per artifact. After write, Read from disk, assert change present. Verified disk state IS the witness, not the green tool call. Discrepancy → fix at root, re-emit, re-verify.

Artifacts the PRD names. Closing narrative → commit message + next `memorize-fire`. Nowhere else.

## Closure Anti-Shapes

See entry. At EMIT the hardest-firing shape is unsolicited docs: filenames matching `<COMPONENT>-{SPEC,SUMMARY,PLAN,ROADMAP,STATUS,NOTES,COMPLETE}.(md|txt)` anywhere — root, `docs/`, subdirs. Variants `START-HERE.md`, `COMPLETED.md`, `SUMMARY.txt` fall under intent.

`"✅ Work Accomplished / 📋 Key Findings / 📁 Deliverables / Session Complete"` prose composed in lieu of writing the artifact is forced closure. Effort estimates as stop-grounds ("Phase 1: 4-6h, Phase 2: 8-12h") are the same shape. Chain is COMPLETE when plugkit emits `transition`, never when the agent narrates.

## Dispatch

`transition` when every planned artifact is written and disk-verified. New unknown → `transition` back to PLAN.
"#;
