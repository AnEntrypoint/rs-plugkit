pub const TEXT: &str = r#"# PLAN

L1 baseline + L2 covering family. Prior memory loads on entry.

## Orient

First non-trivial dispatch = parallel fan-out of `recall` + `codesearch` against the request's nouns, single message. Hits = baseline; misses delimit fresh ground. Proceeding without orient commits to an unobserved envelope.

## Cover

PRD = central plan-item store (`|F|=1`). Content = closure of the destructive transform admissible over the session, enumerated as dependency DAG. Reach permits the next node; the next node is in-scope. Plan-item naming a smaller-than-necessary slice while a larger reachable shape exists = non-monotonic. Partition along dependency edges, not schedule. In-spirit reachable residuals expand the PRD silently; the read declares itself in one line.

Inline TODO in response body violates `|F|=1` and produces unreconcilable state.

## Mutables

Unknowns enter `.gm/mutables.yml` at `status: unknown`. Witness = `file:line`, codesearch hit, exec output. Narrative resolution rejected. Unwitnessed rows block transition.

## Dispatch

`recall`, `codesearch`, `prd-add`, `mutable-add`, `mutable-resolve`, `transition`.
"#;
